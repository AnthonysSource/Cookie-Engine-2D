#pragma once

namespace Cookie {

	template <typename T> using TVector = std::vector;
	template <typename T> using TBitset = std::bitset;
	template <typename T> using TSet = std::set;

} // namespace Cookie